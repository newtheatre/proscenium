{
  "name": "nnt-unified",
  "private": true,
  "type": "module",
  "scripts": {
    "migration:export": "./migration/export.sh",
    "migration:dry-run": "bun migration/inventory.ts && bun migration/transform-identity.ts && bun migration/reconcile.ts"
  },
  "devDependencies": {
    "wrangler": "4.33.1",
    "@types/bun": "1.2.21"
  }
}
