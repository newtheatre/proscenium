# Known issues

Defects and gaps found and not fixed, with where the fix belongs. A problem recorded here is one
somebody chose not to solve yet; a problem nobody wrote down is one the next person rediscovers the
hard way.

Add a row when you find something you are not fixing in the same pull request. Delete it when it is
fixed, in the pull request that fixes it.

## Product

| What | Why it matters | Where it belongs |
| --- | --- | --- |
| **No per-IP rate limiting anywhere except reservations.** A-101 criterion 5 (10 registrations an hour per IP) and A-103 criterion 4 (20 sign-in attempts per 15 minutes per IP) are both unmet. Only the per-address halves are enforced. D-104's reservation endpoint is the first to read the client IP and limit by it. | One address is limited; one attacker with a list of addresses is not limited at all. | A-101, A-103. `clientIp()` in `server/utils/request-ip.ts` already reads `CF-Connecting-IP`; each needs only to call it. |
| **`VERIFY_TOKEN_HOURS = 24` is hardcoded** in `server/utils/tokens.ts`, and J-104 criterion 1 says no policy number is. | It is a policy number the committee may want to move, and moving it is a deploy. | It has no key on the workshop register, so it needs one proposing before it can be configuration. |
| **`ADMIN_TOKEN_HOURS` has no consumer.** The key and its default exist; nothing issues a `SET_PASSWORD` token. | Nothing is broken, but the surface lists a setting that changes nothing. | A-116 and A-121, which introduce the paths that issue one. |
| **An unclaimed console-created account lives indefinitely.** The unverified-expiry sweep exempts password-less accounts, because a guest or console creation was never anybody's registration to complete (0026). | Nothing removes an account made from the console and never claimed, so the exemption is a hole rather than a carve-out until claiming exists. | A-116, which builds claiming and the expiry that belongs with it. |
| **An erased Fellow's citation still names them.** The roll keeps the citation through an erasure, which is what A-127 says to ship: it is public wording the theatre published at the time, and the award is the theatre's record rather than the person's. | It is the one place an erased person's name survives by design, and nobody has confirmed that is what the committee wants. | The committee, who A-127 asks to confirm it. Changing the answer is one line in the personal-data registry. |
| **A Google sign-in still names one account state.** `?refused=linked-elsewhere` says the Google identity is on another account. The disabled state no longer leaks (A-122 criterion 2), but this one does. | It is useful copy for the person and a fact about an account for anyone else. Lower stakes than disabled-ness, which is why it was left. | A-123, which builds the merge that refusal points at. |
| **Personal data is classified per table, not per column.** `shared/utils/personal-data.ts` says what each table holds and what erasure does to it; `docs/data-model.md` promises a per-column scrub classification. `ledger_entries` is the first table where this is actually lossy: it names a person three times (`actor_id`, `comp_approved_by`, `tab_debtor_id`) and the registry can key it on one. | Harmless today, because the ledger's classification is `keep` and all three columns are therefore treated alike. It stops being harmless the first time a table naming a person twice wants different treatment for each. Where two columns already want different treatment, as `external_requests` and `room_bookings` do for their member and their officer, the workaround is a second registry entry for the same table keyed on the other column, with `section: null` so it stays out of the data export. | K-109. The registry would take a list of columns per table rather than one, and the export and erasure queries would build from that list. |
| **No content page names a configuration token yet.** D-103 gives `content/` its first four pages, none of them policy pages, so `check:content-tokens` still passes over zero `{{TOKEN}}` occurrences. | The check runs against real markdown now rather than an empty directory, but has still never refused a bad token. | J-110, whose policy pages are the first to carry one. |
| **A sold-out performance offers no waiting list.** D-101 criterion 2 asks for the waiting-list entry point where the Book button would be; the listing says to ask the box office about returns instead. | Somebody who would have joined a list refreshes the page instead, which is the behaviour D-113 exists to end. | D-113, which builds the list and replaces that line with its form. |
| **Warnings do not yet reach a ticket or a show-night screen.** D-102 criterion 4 says warnings and age guidance flow from these rows to the e-ticket and module E, never re-entered. The rows are in place and nothing reads them there. | Nothing is duplicated yet, which is the risk the criterion guards against; it only becomes a defect if D-108 or an E screen re-enters them. | D-108 for the e-ticket, and the module E screens that show what is on tonight. |


| **C-112 criterion 4 tells the member, and nobody else.** Approvers are not told about short-notice cancellations inside a configurable window. Criterion 3, the externally arranged room, is now closed: withdrawing a union request the SU already holds notifies the approvers, because there is finally an arrangement object to withdraw (C-120, 0036). | An officer learns about a booking freed the night before by looking. | A configuration key for the short-notice window, which has no proposed value in the workshop register. |
| **A no-show cannot be recorded against a union room.** `room_no_shows.booking_id` references `room_bookings`, and a union request is not one (0036). | A member who does not turn up to a room the union gave us costs the theatre its standing with the union and nothing here. | C-116 would need a polymorphic reference or a second table, both of which are worse than the gap while union requests are rare. |
| **A union request has no maximum span and no minimum.** `judgeExternal` checks membership, the past, the notice window and the horizon, and nothing else: a member may ask the union for a room for a fortnight. | The union's own form is the only thing that would refuse it, and by then a person has typed it out. | A follow-up on C-120. The estate's `MAX_BOOKING_HOURS` is about rooms we control and their opening hours, so reusing it would be asserting a rule the union never told us; the window the union will actually accept is a workshop question. |
| **A member picks their own priority tier**, so choosing PRODUCTION for a general rehearsal makes their own booking un-bumpable. | The order is enforced but self-declared, which is most of the way from "published and ignored" to "enforced" and not all of it. | C-115 follow-up: derive the tier from the purpose through a configurable map, with an officer override. |
| **An SU contact is free text about a third party** and is classified by nothing: `external_spaces.contact` and `rooms.contact` both hold "who to ask", which may name a person at the union. | Nobody outside the theatre is in the erasure registry, because nobody outside the theatre has an account. | K-109, if the registry ever grows a notion of third-party contact data. |
| **C-113 is a confirmation and a cancellation, not the story.** Rejection, bump, blackout and expiry notices, coalescing several changes into one message, and the day-before reminder with its ICS attachment are all unbuilt. | Room bookings are one notification topic, so a member who mutes it mutes everything about rooms including the parts not yet built. That is the intended shape, not a gap. | C-109 for the approver notices, C-104 for the ICS, C-114 and C-115 for the blackout and bump notices, and a scheduled task for the reminder. |
| **Cancelling a performance neither refunds nor notifies.** D-121 criterion 5 asks for both, and the cancel route only counts what is owed and records it. `reservations` and `tickets` now exist and the count is real, since D-104 classified `tickets` in the registry; nothing yet refunds or messages a holder. | The criterion reads as met from the backlog and is met only in the count. | D-116 wires the refund workflow into the cancel route, and D-107 or H-105 sends the holders their message. |
| **A desk reservation cannot yet record its booking-window bypass.** `saleRefusal(..., 'DESK')` permits the bypass D-112 criterion 3 asks for, and D-104 built the `window_bypassed` column it is recorded on; nothing writes a `DESK` reservation yet, so the column never turns true. | The predicate and the column are both built and pinned; nothing exercises either. | Whichever story adds a desk-side creation route: D-114 (collection) and D-115 (walk-up sales) both write reservations without one yet existing to bypass. |
| **Nothing shows "booking closed" to the public yet.** D-112 criterion 4 asks the listing to change the moment the window passes. There is no listing. | The window is computed per request rather than stored, so a listing built on `saleRefusal()` is right by construction; nobody has built one. | D-101, which must compute the state per request rather than cache it past the window. |
| **Venue, season and category administration have no MVP story.** `GET /api/admin/venues` reads the venues a performance may be put in, and nothing creates or edits a venue, a season or a show category outside the seed. E-113 covers the emergency card alone. | A second venue can only be added by hand against the database, which is not something a committee should have to do. The show screen round-trips `category_id` and `season_id` unchanged so a full replace cannot silently clear a seeded or imported value, which is a workaround and not a surface. | Nothing yet. Each needs a story, and all three tables already have every column they would want. |
| **A choice group can be created and attached, but not from a screen.** F-113 built `POST /api/admin/bar/choice-groups` and `PUT /api/admin/bar/variants/[id]/choice`; the recipe editor on `/bar/products/[id]` still only manages direct stocked-ingredient lines. | A bar manager cannot set up a spirit's mixer choice without an API client, so 0017's free mixer stays unreachable from the console even though the write path is proved. | No MVP story owns this screen yet; F-103's till is what actually needs choice groups to exist, so the admin screen can land alongside it or as its own small follow-up. |
| **A product may go `ACTIVE` with a variant and no recipe line at all.** F-111 criterion 2 reads "at least one variant and a resolvable recipe"; `BAR_PRODUCT_REFERENCES` only requires a serving size, and F-113's `retiredIngredientsOf` only refuses a recipe that already names a retired item, never an empty one. | A product can reach the till with a size button that adds to the basket but depletes nothing, which no sale path can resolve once F-105 exists. | Undecided whether "resolvable recipe" means "has one" or "what it has resolves"; a committee question before whichever story enforces it. |
| **Retiring a stocked item never checks whether an already-`ACTIVE` product still calls for it.** `retiredIngredientsOf` and `retiredOptionsOf` (F-113) only run on a product's own transition to `ACTIVE`; `items/[id]/status.post.ts` retires an item with no check of what depends on it, directly or through an attached choice group. | An `ACTIVE` product can end up with a recipe line, or a choice option, that nobody will ever be able to pour, and nothing about the product's status says so. | Whichever story first re-checks an `ACTIVE` product on read rather than only at the moment it goes active; F-103's till is the first reader that would notice. |
| **The stocked-item retirement guard reads on-hand before the write, not on it.** `items/[id]/status.post.ts` checks `held.onHand !== 0` and then writes an unconditional `UPDATE`, so a delivery or stocktake landing in the gap between the two lets the retirement commit against an item that now carries stock. | Rare and narrow: the window is one request's round trip, and nothing yet writes stock outside a bar-manager action. Becomes a real race the day F-115's stocktakes or a delivery screen can run concurrently with a retirement. | Whoever next touches this route should fold the on-hand predicate into the `UPDATE` itself, the shape 0049 already uses for `performances/[id]/index.put.ts`'s capacity check, rather than reading it first. |
| **The product status route's `everSold` and `missingBeforeActive` guards are read before the write, not on it.** `products/[id]/status.post.ts` checks both, then writes an unconditional `UPDATE`; a sale or a variant retirement landing in the gap lets a status change commit against a product that is no longer eligible, un-retiring a sold product or activating one whose last usable variant just went. | Live now that F-105 posts real sales, and compounded by a second gap: `held.everSold` here is `BAR_PRODUCT_REFERENCES`-driven, which has no entry pointing at a sale, direct or through a variant, so it reads false even for a product genuinely sold via one of its variants. `VARIANT_REFERENCES` gained its `ledger_lines` entry with F-105; `BAR_PRODUCT_REFERENCES` did not, because a product's sale is two hops away (through `product_variants`) and the completeness pattern here assumes one. | Same fix as the stocked-item guard above for the staleness half. The vacuous half needs `BAR_PRODUCT_REFERENCES` (or `productEverSoldColumn`) taught to look through a variant to `ledger_lines`, which no story owns yet. |
| **A pass reads as never issued and never live against any show.** `PASS_TYPE_REFERENCES` and `PASS_COVERAGE_REFERENCES` in `server/utils/pass-types.ts` are both empty until D-124 builds `passes`, so deleting a pass product never actually refuses (D-123 criterion 3) and dropping a covered show never actually needs the manager permission (D-123 criterion 4). Both predicates are proved against a stand-in table in the integration suite. | Nothing is sold yet, so there is no window where either reads wrong; both start refusing correctly the moment D-124 adds a row that references a pass type. | D-124, which adds `passes` and must classify it in both registries. |
| **A pass's cap is stored and not enforced.** `pass_types.max_issued` is configuration only; D-123 criterion 2's 409 belongs to the issuing write path, which does not exist. | The blunt guard is data, not yet a guard. | D-124, which must check the cap atomically at payment, not at request. |
| **Door scanning and reports say nothing about an externally ticketed performance.** D-122 criterion 2 asks the door and reports to state it plainly rather than showing misleading zeros; only the box office console does so today, because neither the door nor a report exists yet. | The internal sales paths that exist (the desk's on-sale toggle) already refuse and quote the link; nothing internal shows a wrong number yet, because nothing internal reads a performance's figures yet outside the console. | D-126 for the door, and whichever module builds the first show-night or box office report. |
| **The editorial pages ship as plain markdown with no editing surface.** D-103 criterion 2 asks for a git-backed visual editor in the Nuxt Studio style, with draft, preview and publish states; committee direction on 5 September 2026 deferred it rather than take `nuxt-studio` or any other new runtime dependency inside a content story. A committee member without a Git workflow can only ask a developer to edit a file. | The page structure, routing and token pipeline are real; only the non-technical editing surface is missing. | Its own story, needing a spike against the `cloudflare_module` Nitro preset first: an editing surface here becomes the pattern every future content screen inherits, so it is validated once rather than per page. |
| **The four editorial pages carry placeholder copy, not the committee's words.** D-103 ships `content/about.md`, `content/history.md`, `content/get-involved.md` and `content/technical-specification.md` with every section marked `placeholder: true` and stating plainly that it awaits the committee; committee direction on 5 September 2026 was not to invent a real theatre's venue dimensions, history or recruitment copy, and not to import the old estate's text either, because the criterion asks for it rewritten. | The public site names the theatre honestly rather than plausibly, but nothing on these four pages can be relied on yet. | The committee, who `docs/workshops.md` now asks for the venue specifications, the history and the get-involved copy; a routine content edit once supplied, needing no code change beyond clearing the flag. |
| **Nothing yet reads `doorWording()` or offers the access ticket type.** D-127 builds the profile, its verification and the pure gate (`shared/utils/access-profiles.ts`'s `doorWording`, pinned by unit tests); no scan reads it and no booking flow checks a caller's verified, consented, unexpired profile before showing the access or companion ticket types. | Declaring and verifying already work end to end; nothing downstream consumes the result yet, so an access profile has no visible effect on a booking or a scan today. | D-126 for the door read, D-128 for the entitled ticket types and the companion count, E-123 or E-127 for the night report's counts-only rule (D-127 criterion 3). |
| **Closing a till session shows no expected figure.** F-102 criterion 4 asks for one; `/api/tonight/till/close` closes the session and stamps who and when, and nothing else, because there are no sales to compute a figure from. | Closing today is bookkeeping only; a bar manager gets no cross-check against the SumUp Z. | F-118, which is the reconciliation itself and needs F-105's sales to compute from. |
| **A session left open past its night has no screen.** `staleUnclosedSessions()` in `server/utils/till.ts` names every one, and `/api/tonight/till/close` will close any of them for the bar manager's role; nothing lists them for a duty manager to find. | A forgotten session is only discoverable by asking the bar manager to check, not by a checklist surfacing it. | E-114, the close-night checklist, which is unbuilt. |
| **The tonight screen's quick links are missing one of six.** E-112 criterion 4 asks for the incident log, the Challenge 25 register, near-miss reporting, the checklists, the backstage board and close-night, each within one tap; every one but the backstage board (E-120, unbuilt) now exists and is linked. Near-miss reporting reaches from `/tonight/incidents` in a second tap, not a first tap from `/tonight` itself, an interpretation of E-117 criterion 1's "one tap to open" that trades a dedicated action on every show-night screen for one screen that does both jobs. Close-night is the checklist screen's own action, not a separate link. | A duty manager reading `/tonight` still cannot reach the backstage board from the phone screen the story asks it to be reachable from; a near miss costs one tap more than the criterion's own wording asks for. | E-120 for its own screen, and a decision on whether the near-miss interpretation stands or needs its own action on every show-night screen. |
| **The Challenge 25 register's till-inline and door reachability are still open.** `/tonight/age-checks` closes the standalone half of E-118 criterion 4; the till-inline half is F-106's own seam (`recordAgeCheck()` already supports it), and the door half waits on a door screen existing at all, which no story has built. Tracked on issue #457, reopened rather than left closed against an unmet criterion. | A bar sale of an age-restricted item still round-trips to `/tonight/age-checks` by hand rather than prompting inline, and nothing at the door can log a check at all. | F-106 for the inline half; whichever story first builds a door screen for the other. |
| **A Challenge 25 check needs something running tonight to authorise, even though the entry itself does not.** `requireAnyNightAuthority()` resolves through `coverage()`, which refuses when nothing is on the programme for the night (E-111); E-118 criterion 4's "bar can check outside a show" is true of the row (`performance_id` is nullable) but not of who may write one; on a night with nothing scheduled at all, no officer bypass has anything to take charge of and the route refuses. The same limit now holds for `incidents` (E-115, E-117), built on the same authority guard. F-106's inline prompt on `POST /api/till/sale` does not hit this: it rides the sale's own already-resolved authority rather than resolving its own, and a till session cannot open without a performance backing it either, so the standalone route (`POST /api/tonight/age-checks`) is where this gap actually lives. | A social night with a licensed bar and no performance on the programme cannot use either register at all, which is exactly the case E-118 criterion 4 names. | Whichever story first needs authority that does not derive from a performance; until then this is the honest limit of E-111's own model, not a bug in either module's routes. |
| **An incident's free text can name somebody an erasure has already scrubbed.** `incidents.reported_by` is kept on erasure, the same reasoning `age_checks.checked_by` survives on; but E-115 criterion 5 also asks that free text referring to an erased person be scrubbed, and the mechanical erasure system only matches a single FK column against the erased id, never scans `body` for an arbitrary name. | An erased member's name can persist inside somebody else's incident account indefinitely, which criterion 5 exists to prevent. | K-109's erasure system would need a text-scan or a redaction pass over free-text columns, which no table's erasure treatment does today; a design question before a story owns it. |
| **The incident log has no "safety officer" scope, and no cross-night historical read.** E-115 criterion 4 names the safety officer as a reader alongside tonight's shift authority, the FOH officer and administrators; `shared/utils/roles.ts` has no such role, and `GET /api/tonight/incidents` is scoped to tonight only, the same scope E-118's register already carries. | Nobody can read a past night's incidents or near misses outside the database, and nobody holds the role E-116's routing and E-126's trend queries are written to notify. | E-116, which needs the role before it can notify anyone; a committee question on whether "safety officer" maps to an existing role or needs its own. |
| **`/api/tonight/till` cannot name a venue for you.** Like `/api/tonight/authority`, a night with more than one venue running refuses with a 400 asking which one; `/tonight/till` has no venue picker to answer with. | A single-venue night (today's only case) works from the screen; a two-venue night needs the API called with `venueId` directly. | Whichever story first builds a venue picker for a show-night screen; none has yet. |
| **Retiring a stocked item checks stale stock.** `server/api/admin/bar/items/[id]/status.post.ts` reads `held.onHand` before the write and refuses retirement if it is nonzero, but the `UPDATE`'s predicate only re-checks `status`, not `on_hand`. A movement landing in the window between the read and the write is not covered. | A count or a delivery arriving between the read and the write can retire an item that, by the time the write lands, has stock on hand again, which is the exact state F-114 criterion 1 exists to refuse. | F-114's own route. The predicate would need `on_hand = 0` carried onto the `UPDATE`, with the refusal re-reading current stock the way 0049's response half now does for a lost status race. |
| **Retiring or activating a product checks stale sale and completeness state.** `server/api/admin/bar/products/[id]/status.post.ts` reads `held.everSold` and, on activation, `missingBeforeActive(id)` before the write; the `UPDATE`'s predicate only re-checks `status`. A sale or a variant change landing in the window is not covered. | A sale posted between the read and the write can un-retire a product that has in fact been sold, or a variant retired in the same window can activate a product that no longer has one, both invariants F-111 exists to hold. | F-111's own route. Each check needs its own condition folded into the `UPDATE`'s predicate rather than read once and trusted. |
| **A released hold does not yet offer the waiting list.** D-106 criterion 2 asks a freed ticket to be offered to the waiting list in order; `releaseExpiredHolds()` only moves the reservation to `EXPIRED`, because `waiting_list` does not exist. | The seat does return to sale the moment the status changes, since capacity counts `PENDING\|COLLECTED\|DOOR` reservations only; nobody on a list is told, because there is no list. | D-113, which builds the list and is the one that must call into the release path when it lands. |
| **The no-show-holds system check has nothing that can ever clear it once tripped.** `noShowHoldsReleased()` (E-114) reads whether any reservation for tonight's performances is still `PENDING` or `COLLECTED`; only a door scan resolves one to `DOOR` or `NO_SHOW`, and D-126 has not built the door yet. | A venue selling tickets cannot close a required item naming this check without exempting it every single night, which is not what "system-verified" is meant to relieve a duty manager of. | D-126. Until it exists this is the honest limit of the check, not a bug in E-114's own routes. |
| **The exception path's reason has nowhere to be read except the database.** E-114 criterion 5 asks that every exception "print in the night report" and "appear in the FOH officer's digest"; `checklist_stamps.exempt_reason` records it, and neither `night_reports` nor a digest exists yet to read it (E-123, E-124). | An exception is fully audited but invisible to anyone who is not looking at the database directly; the committee oversight the criterion asks for does not yet exist. | E-123, which would project `checklist_stamps` into the night report it compiles; a notification story for the FOH digest half. |
| **A stale till session left open past its night has no checklist item asking about it.** F-102's `staleUnclosedSessionsQuery` exists precisely for E-114's checklist, but criterion 3 names only two system-verified checks and neither is this one, so it was not added as a third. | A duty manager closing the night is never told a till from an earlier session is still open. | Whichever story adds it: either an E-114 follow-up, or a manually-configured item pointed at a future admin-facing query. |
| **A tab charge's outstanding balance never falls, because nothing yet settles one.** F-108 reads `ledger_entries.tab_settled_at IS NULL` to sum what a holder owes; the column exists but no route ever sets it, since settlement is F-109's story. | Every tab charge simply accumulates against the cap forever; a holder near the cap has no way to pay one down until F-109 exists, which is the honest limit of shipping F-108 first, not a bug in its own write path. | F-109, which needs to mark the charges a settlement covers, bounded at initiation per its own criterion 3. |
| **The QR cannot be saved to Apple Wallet or Google Wallet.** D-108 criterion 3 asks for both; a real `.pkpass` needs a paid Apple Developer certificate and a Google Wallet needs a Google service account, and neither exists in this environment. `/qr` shows the code as an image instead, which can be saved or screenshotted like any other picture. | A booker can keep and reuse the code, just not through either wallet app's own pass mechanism. | Whoever holds the Apple Developer and Google Wallet credentials; the signing and issuing code has nowhere to plug in until then. |
| **The QR retrieval route cannot yet tell "exchanged" or "wrong night" apart from "unpaid" or "cancelled".** D-108 criterion 5 asks for five loudly distinct states; `qrStatusDisplay()` covers the four `RESERVATION_STATUSES` already live (unpaid, paid, cancelled, no-show/lapsed). Exchanged needs D-111, which does not exist; wrong night needs a door screen with a night in context, which is D-126's. | Presenting the code today never says "exchanged" or "wrong night", because a reservation cannot yet be in either state. | D-111 for exchange, D-126 for the door screen that knows which night it is. |

## Platform

| What | Why it matters | Where it belongs |
| --- | --- | --- |
| **Imported ticket money carries no performance link.** `ledger_lines.performance_id`, `.reservation_id` and `.ticket_id` are left unset on every row `migration/transform-money.ts` writes (K-114, I-109): no programme transform exists yet to map the old estate's performance ids to the live ones, and 0015 forbids carrying the old ones through unmapped. | Revenue by show (I-106) cannot attribute this imported history to a performance; only the total and the per-day/per-season reconciliation are complete. | A later programme transform could backfill `performance_id` from `out/money-id-map.tsv` (old ticket id to ledger entry id) joined against the old estate's own `performance_id` on that ticket, but **only while `out/money-id-map.tsv` and the archived old estate both still exist**. That is a dependency on an archive staying available, not on code, and it is not enforced or checked anywhere: the day the archive goes, the link becomes silently unrecoverable. |
| **Two scheduled tasks are stubs.** `sessions:sweep` and `nights:close` are registered, run on their cron and do nothing but name the story they wait for. `training:expiry-sweep`, `shifts:remind`, `backup`, `holds:release` and `retention:sweep` were five of them and now do their work (G-125, E-109, K-108, D-106/D-107, K-111). | A cron that fires and does nothing looks identical to one that fires and works. | Each named story. |
| **Retention arming has no preview, typed confirmation or one-action revert.** J-105 criterion 4 (refuse arming without a reviewed dry-run digest) is enforced: `PUT /api/admin/config/RETENTION_ARMED` checks a digest has actually sent. Criteria 1 to 3 and 5, the wide-blast-radius flag, the live preview and the typed confirmation that names what it is confirming, are not built; `RETENTION_ARMED` is settable through the same generic route as any other key. | Arming today is one PUT with no preview of who is affected and no confirmation beyond holding `config.write`, which is thinner than J-105 asks a wide-blast-radius setting to be. | J-105, unbuilt. Planned for December in any case (roadmap), after the first reviewed digest, so the gap is not urgent, but the route should not be mistaken for the finished flow. |
| **The end-to-end suites fail on a GitHub runner and pass locally.** Sharing one dev server across the suites is green locally; on CI the server answers 500 from the third suite onwards and a hundred and one tests fall over. Three causes were tested; two were real defects fixed on the way and neither was it. | The nightly end-to-end workflow is red until this is understood, so a browser regression would not be caught there. | Lock contention on the shared database is now the leading candidate, because it is what made the suites unstable locally and the symptom is the same shape: the server's own writes fail while a reset holds the write lock. Untested on a runner. The next step is re-running the nightly workflow against the reset's busy timeout, and only then capturing the server's output, which the harness still discards. |
| **Every guard reads `liveGrants` twice** for the same account in one request: `authority()` reads it, then `requireSecondFactorIfPrivileged` reads it again. The training guards add a third query for `liveLeads`. | Two D1 round trips where one would do, on every guarded request, and three on a training one. | Whenever the authorisation path is next touched. |
| **A second factor is not required of authority that derives.** `requireSecondFactorIfPrivileged` weighs granted roles, so a department lead holding no role edits their catalogue, and a trainer holding no role schedules a session, without one (0037, A-112). | A lead editing safety-critical modules, or a trainer scheduling one, is held to a weaker bar than an officer doing the same. | Undecided: whether derived standing should carry the MFA requirement is a committee question, not a bug to fix quietly. |
| **The night cache is built and no screen uses it.** `useNightCache` and `shared/utils/night-cache.ts` hold K-103's rules and are pinned by unit and integration tests; the screens K-103 criteria 1 and 2 name (door, till, tonight view, emergency card) do not exist, so nothing asserts a full render in a browser with the network stubbed to fail. | The mechanism is proven and its use is not, so K-103 reads as met from the backlog while no screen has ever rendered from cache. The placeholder hub could not stand in: listing tonight's programme needs a read route in show night's `/api/tonight/**`, and the one route that exists there writes an officer-bypass audit row, which 0044 makes a deliberate act rather than a side effect of opening a page. | D-126, F-103 and E-113, each adopting it for its own payload and carrying the browser case for its own screen. |
| **A cold load with no network at all still fails.** The night cache renders a running application from the device; fetching the document itself is a network request, so a screen opened from a dead tab with no connection gets nothing to run. | K-103 criterion 2 is about a screen losing its connection, which is covered, and it is easy to read it as covering a phone that has been locked all evening, which is not. | The service-worker question, open in `backlog/K-platform.md` and shared with K-104. It is the only thing that would serve the document offline, and a stale service worker on show night is its own risk. |
| **Five external-request routes still audit outside the batch.** `assign`, `submit`, `relist`, `refuse-assignment.post.ts` and the member's own `cancel.post.ts` (`server/api/(admin/)rooms/external-requests/[id]/`) each await `moveRequest()`, a conditional `UPDATE ... RETURNING id`, then write a separate, unbatched `db.insert(schema.auditLog)`. This is the shape 0049 rejects: `reject.post.ts` in the same directory was swept onto `auditedWrite` (0049), but these five were never named by that record's own survey. | A losing racer's `UPDATE` touches nothing and `moveRequest()` returns `false`, so the route already refuses correctly; the gap is narrower than the routes 0049 fixed. But `assign.post.ts` also writes `externalAssignments` in a third, separate round trip after the move succeeds, so a process ending between the move and that insert leaves a confirmed request with no assignment row, which is a real gap beyond the audit question. | `server/utils/external-requests.ts`'s `moveRequestStatement` already returns a `db.batch`-ready statement; each route wants `auditedWrite(moveRequestStatement(...), entry)` in place of its two separate calls, and `assign.post.ts` wants its `externalAssignments` insert folded into the same batch, not just its audit. |
| **D-104 widened the blast radius of a lagging migration.** Classifying `tickets` as `sold: true` (D-105's registry) flips `performanceSoldColumn`, `performanceSoldQuery` and `showSoldColumn` from a literal `SELECT 0` touching no tables into real queries joining `tickets` and `reservations`. `server/utils/whats-on.ts` calls `performanceSoldColumn('p')` with no explicit references, so it inherits the live registry, and that function backs the public, unauthenticated `/whats-on` and `/shows/[slug]` pages. | Before D-104, a Workers Builds deploy that outran the migration job could only break the new booking routes, which nobody was using yet. After it, the public homepage and every show page throw `no such table: tickets` until the migration catches up, because proscenium's deploy and its migration job start from the same push and race, winning their ordering by timing rather than by guarantee (`docs/operations.md` "What it cannot do"). `/api/health` returns 503 naming the pending file, and `health:watch` now alerts the IT Manager if it stays that way (J-106), so the gap is noticed rather than silent; it is not closed. | Not a defect to fix from a box office pull request: actually sequencing the migration against the deploy, rather than alerting once it has not, is K-107's. |

| **A build while `bun run dev` is running breaks the dev server.** The build recreates the local database file, and every write from the running server then fails with `SQLITE_READONLY_DBMOVED`. | The error names neither the cause nor the fix, which is to restart `bun run dev`. | Nothing yet: it would want the dev database and the build's to be different files. |

## Tooling and tests

| What | Why it matters | Where it belongs |
| --- | --- | --- |
| **A held port used to abort a run that had already announced itself.** `scripts/run-tests.ts` printed "end-to-end: N suites" before checking the port, so a refusal looked like a finished run to anything watching for that line. Fixed by checking first, but the banner and the summary still share a prefix. | Two runs were read as results before this was found. | Fixed for the abort case. A watcher wanting a completion sentinel should match "end-to-end: pass" or "end-to-end: FAIL", not the prefix. |
| **`Bun.WebView` exposes `click`, `type`, `press` and `cdp`**, and `tests/helpers/webview.ts` reimplements the first two through `evaluate`. | The bespoke versions work and are tested, but they are bespoke: the native ones dispatch trusted events, which ours cannot. | A tidy-up when a test needs something `evaluate` cannot do, such as a real keypress. |
| **One Chrome profile leaks per test run**, about 130MB, from the probe in `skipReason()`. The rest are swept when the app stops. | It filled this machine's `/tmp` with 105 of them before the sweep existed. One a run is survivable; nothing cleans it. | The harness, when the probe can be made to report its directory. |
| **Registration's "asks the visitor to check their email" (A-101) fails on a cold `.nuxt`, in a full run.** It passes warm and passes alone. A cold full run fails it more often than not, but not on demand. | One of the browser cases covering registration is unreliable, so a real regression there could hide behind a run everyone has learned to re-run. | A cold `.nuxt` and a full run is the best trigger anybody has, and it is not reliable enough to bisect against. Instrumenting what the component emits, rather than reading the DOM, is what found the fellowship half of this (0039), and is the next step here. |
| **Two end-to-end runs on one machine kill each other.** `E2E_BASE_PORT` defaults to 3101, so every run binds the same port, and the runner refuses to start while it is held. The obvious unblock, `fuser -k -n tcp 3101`, does not ask whose server it is: it takes out whatever run is in progress, whose suites then fail with `ECONNRESET` and `ConnectionRefused` from that point on. | A run killed at suite 7 looks like forty failures in modules nobody touched, and the first instinct is to blame the branch. It is not a defect in the runner and there is nothing here to hunt. There is a worse shape of this: a leaked dev server from **another worktree** answers on 3101 and the runner accepts it, because it is a proscenium app and passes the is-this-our-app check. The suite then runs against a checkout that does not have the branch's routes and every call 404s, which reads like the routes were never written. | **Set `E2E_BASE_PORT`** to something of your own when anybody else might be running, at least the shard count apart. Before clearing a stuck port, `ps -eo pid,args \| grep "[r]un-tests.ts"` and see whose it is. A run interrupted this way tells you nothing about the branch: re-run it. |
| **A dev server blocks a run on a different port, because the lock is the checkout and not the port.** `nuxt dev` takes one lock per checkout, so any dev server running in a worktree refuses every other dev server in that same worktree whatever port either was given. A run on a private `E2E_BASE_PORT` still fails, and because the harness discards the server's output the only symptom is `server at http://localhost:<port> did not become ready within 120000ms`, once per suite. | The two documented defences do not cover it: setting your own port does not help, and `ps -eo pid,args \| grep "[r]un-tests.ts"` finds nothing, because no test runner is alive to find. What holds the lock is usually a leaked server nobody is waiting on, including one a tooling process booted on the default port and never killed. Read literally, the row above says a run on your own port is safe, and it is not. | `ps -eo pid,args \| grep "[n]uxt dev"` finds them all, and `ls -l /proc/<pid>/cwd` says which checkout each belongs to; one in your own worktree with no runner alive is yours to kill. Running `nuxt dev` by hand is what makes the lock say so, naming the holding pid and directory. A harness that surfaced the server's first lines, which known issues above already want for another reason, would have said this in one run rather than three. |

| **Only one training gate exists, so gate signposting is only a third built.** G-102 criterion 3 and G-101 criterion 3 both ask every gated surface to name the module that would unlock it, and to treat EXPIRING as held. The prerequisite gate on a sign-off does both. Shifts, the till and kit loans have no gate to signpost yet. | Nothing is wrong today, but both criteria read as met when they are met only where a gate exists. Somebody ticking them off from the backlog would be wrong. | Modules E (shifts), F (the till) and C (kit loans). Each closes its own third as it builds its gate; `modulesHeldBy()` and `heldNow()` are what they call. |
| **One named regression case is still a `test.todo` stub**, in `races-stock`, split by invariant since K-105 so two streams filling them do not collide on one file. CI counts it as passing. `races-capacity` and both cases in `races-shifts` are now filled. | K-121 claims a named regression suite that gates from day one, and this one is still empty. `money` (now `money-*`) is fully built and `erasure` was filled in as of K-109. | K-121, and F-105, the story that makes the case buildable. |
