---
title: Stocktakes
description: Counting the bar against what the register expects, telling a blank from a nought, and applying the variance.
module: Bar
updatedOn: 2026-09-15
updatedBy: Matt Adcock
navigation:
  icon: i-lucide-clipboard-list
---

A stocktake is a count of the bar against what the movement history says should be there. It is
at **Manage, Bar, Stocktakes** and needs `bar.read` to open past stocktakes and `bar.write` to
open a new one, count into it or apply it, both held by the Bar manager role. The variance is
never hidden and never corrected on its own: it is shown as it is counted, and written to stock
only when somebody deliberately applies it.

::callout{icon="i-lucide-info" color="info"}
**A blank count and a count of nought are different things.** An item you did not count stays
blank and posts nothing when the stocktake is applied. Only a figure you typed, including nought,
counts.
::

## Opening a stocktake

1. Press **Open a stocktake**.
2. The screen opens the new stocktake. If one is already open, you join it instead, and the
   message says so: there is one open stocktake at a time.

Opening captures the expected on-hand figure for every stocked item that is not retired, at that
moment, so a sale after this cannot muddy the comparison.

![The stocktake list with the open button (1) and the count or view link (2)](/images/docs/bar/stocktakes.png)

1. **Open a stocktake** starts a new count, or joins the one already open.
2. **Count** opens an open stocktake; **View** opens an applied one.

The list shows each stocktake's status, **Open** or **Applied**, when it was opened and when it
was applied. The toolbar searches and filters on the status and sorts by when it was opened,
newest first.

## Counting

![One stocktake with the uncounted total (1), the lines (2) and a variance cell (3)](/images/docs/bar/stocktake.png)

1. **How many items are not yet counted**, beside when the stocktake was opened.
2. **The lines**, one per stocked item, with what was **Expected** when the stocktake opened.
3. **Variance** is the counted figure minus the expected one, in the item's unit, with the cost
   of that difference in the **At cost** column once it has been saved.

Counting is a loop of typing and saving, in any order, over as many visits as it takes.

1. Walk the bar and type each item's count into **Counted**, in the item's own unit. Leave an
   item blank if you did not count it; clearing a field puts it back to blank.
2. Press **Save counts** as you go. Only the lines you changed are sent, and the variance and
   its cost are shown before anything is applied. Saving with nothing changed says *Nothing to
   save*.
3. Come back and carry on: an open stocktake keeps its counts until it is applied.

## Applying

1. Press **Apply**, then **Apply** again on the confirmation.
2. One adjustment movement is posted for every counted item whose count differs from what was
   expected, all in one write, and the stocktake is frozen for good.

A blank line writes nothing and shows as *Uncounted* afterwards. A counted item that matched its
expected figure writes nothing either. A frozen stocktake is never edited: a mistake is corrected
by a new stocktake or a reversing movement.

## If something goes wrong

- **"This stocktake is no longer open, so nothing here was recorded"**: somebody applied it
  while you were counting. None of that submission was saved; open a new stocktake and count
  again.
- **"This stocktake has already been applied"**: the apply was refused because it had already
  happened. Reload and view the result.
- **"Joined the stocktake already open"**: not a refusal. Only one stocktake is open at a time,
  so you are counting into the one that already exists.
- **"The stocktake did not open"**: the screen tried three times and could not claim the open
  slot. Try again; if it repeats, report the page.

## What happens next

Opening, every save of counts and the apply are written to the audit trail; the trail records how
many lines a save touched, never the figures. Applying posts the adjustments to the movement
history as **Stocktake adjustment** rows pointing at their stocktake lines, so on hand moves at
once on the stock register and the order list, and the variance appears on the reports for the
period the stocktake was applied in.

## Related pages

- [Stock](/docs/bar/stock)
- [Movements](/docs/bar/movements)
- [Reports](/docs/bar/reports)
