---
title: Accounts
description: Finding an account, adding one, granting and revoking roles, and the security operations, merge and erasure that act on a single account.
module: Identity
updatedOn: 2026-09-23
updatedBy: Matt Adcock
navigation:
  icon: i-lucide-users
---

The accounts directory is every person the theatre knows: members, ticket guests, officers and
the placeholders the import brought over. It is at **Manage, People, Accounts**, and the IT
Manager, the Manager, the Theatre Manager and the Training Manager read it. Opening one account
shows how they sign in (and the Workspace address their first Google sign-in will claim), the roles they hold, their membership and fellowship, and the last few
things done by or to them. The actions on that page are the IT Manager's alone: creating an
account, setting that Workspace address, granting and revoking roles, disabling, merging and
erasing.

::callout{icon="i-lucide-info" color="info"}
**Two kinds of account are hidden until you look for them.** An anonymised account (one that has
been erased) and a shadow account (one nobody ever registered: no password, no Google link, no
passkey) stay out of the list until a search, their own filter or the never-signed-in filter asks
for them. The total line says how many shadow accounts were left out, with **Show them** beside
it, so hidden never means lost.
::

## If something goes wrong

- **"You do not have permission to do that"**: the action behind the button is the IT Manager's.
  Ask them. The security card and erasure are not shown to anybody else at all, so this answer
  comes from **Merge into another account**, which is offered to everybody who can open the page
  and carried out by the IT Manager.
- **"This role needs an authenticator app before it can be used"**: your own role is privileged
  and you have not enrolled an authenticator. The alert links to the security page to set one up.
- **"That address already has an account"**: search for it instead; it may be a shadow account
  hidden from the default list.
- **"Nothing can be delivered to that address"**: the address is a placeholder or an
  undeliverable domain, so a set-password link could never arrive.
- **"That address is waiting to be linked to X's account for Google sign-in."** when adding
  someone: that account's first Google sign-in will claim the address, so a new account on it
  would never be signed into. Open X's account instead.
- **"Only a newtheatre.org.uk address can be linked for Google sign-in"**: the Workspace address
  field takes a theatre address only. A personal address signs in without a link.
- **"That address already belongs to X's account."** or **"That address is already waiting to be
  linked to X's account."** when setting a Workspace address: one address leads to one account.
  If X is the same person, merge the two accounts (below) and set the address on the winner.
- **"That account already signs in with Google"**: there is nothing to link. A wrong Google
  identity on an account is a merge, never a quiet swap.
- **"Somebody else linked that address first. Reload the account to see who."**: another
  officer set the same address on another account a moment before you did. Reload, and the
  refusal names them.
- **"You do not have permission to grant roles"**: creating the account is allowed but the roles
  chosen with it are not. Create it without roles.
- **"That account is no longer here."** when granting a role: the account has been erased, and
  a closed account holds no role.
- **"That is the last IT Manager: grant another before revoking this one"** (or disabling,
  erasing, or merging this one away): the theatre never loses its last usable IT Manager. A
  disabled or erased second IT Manager does not count. Grant another first.
- **"That is your own account"**: security operations, erasure and merges refuse to target the
  person doing them, so nobody locks themselves out by accident.
- **"That account has been erased"**: nothing acts on a closed account.
- **"No account matches that email exactly."**: the winner's address must match an account in
  full, and a partial match is not offered: the wrong winner cannot be undone.
- **"An account cannot be merged into itself"** and **"An anonymised account cannot take part in
  a merge"**: pick a different winner.
- **"Type the losing account's email address exactly to confirm"**: the confirmation is checked
  again before the merge runs, character for character once spaces and capitals are set aside.
- **"That account changed since it was last checked: look at it again before merging"**: the
  losing account was altered between the preview and the merge. Reload and preview again.

## Finding an account

![The directory with the search field (1), the filters (2), the Add someone button (3) and the table (4)](/images/docs/people/accounts.png)

1. **Search** by any part of a name, an email address or a student number, then press
   :kbd{value="Enter"}. A search always shows shadow accounts: somebody typing an address
   wants that address, whoever holds it.
2. **Filters** narrow the list to the questions the handover raises: holds a role (narrowable to
   one role), membership current, lapsed or never, address verified, disabled, anonymised,
   authenticator enrolled, privileged with no authenticator, approaching retention, never
   signed in, shadow account, and last seen within a date range. The sort is by name, last seen
   or joined. Search, filters, sort and page all live in the address bar, so a triage list can be
   bookmarked or sent to somebody.
3. **Add someone** creates an account from the console (below).
4. **The table** shows each account's state badges (Anonymised, Disabled, Shadow, Unverified,
   Authenticator), how it signs in, and when it was last seen. Choose the arrow at the end of a
   row to open the account.
5. **The total line** counts what matched and how many shadow accounts the default view hid.

Two banners appear above the list when there is something to act on: the number of privileged
accounts without an authenticator (their roles do not work until they enrol one) and the number
of accounts dormant for longer than {{ RETENTION_FULL_ACCOUNT_YEARS }} years, which are the
ones that would be warned about closing. Each banner's **Show them** applies the matching
filter.

## Adding someone

An account made from the console never gets a password here. The person gets an email with a
link to choose one, and until they do the account is a shadow account, hidden from the default
list: search for it rather than expecting it in the table.

1. Press **Add someone**.
2. Give their **name** and **email address**. A `@newtheatre.org.uk` address signs in with
   Google and gets no link: no password may ever exist on a theatre address.
3. Optionally give a **Workspace address** for somebody whose own address is personal: their
   first Google sign-in with it joins this account instead of making a second one. Leave it
   empty for anybody else.
4. Optionally choose **roles** to grant in the same action, which is the IT Manager's to do.
   Each grant expires at the committee year end.
5. Press **Create the account**. The link in the email lasts {{ ADMIN_TOKEN_HOURS }} hours.

## One account

![An account with its name and state (1), the Role choice (2), the Until choice (3) and the Grant it button (4)](/images/docs/people/account.png)

1. **The name and state.** Disabled, Shadow or Unverified badges sit beside the name. Beneath
   it, **Signs in with** lists the methods held: a password, Google, passkeys, and an
   authenticator with the number of recovery codes left. "Nothing yet" means the account cannot
   sign in. The **Roles** card lists every grant by its title, with its expiry, or "further
   notice" for a permanent one, and any note written with it beneath; a grant that has run out
   reads as lapsed and grants nothing.
2. **Role** offers the roles the account does not already hold.
3. **Until** is the committee year end, a date you pick, or further notice, and a **Note** may
   be written beside it.
4. **Grant it** writes the grant. All of it shows only to the IT Manager; granting is covered
   below. Beneath the form, **Open the register** opens
   [Roles](/docs/people/roles), which is the better door at handover.

**The Workspace address**, under **Signs in with**, is for an incoming officer whose account is
on a personal address. Type their `@newtheatre.org.uk` address and press **Set the address**: the
first time they sign in with Google using it, it joins this account, their own address stays as it
is, and the address is marked as used up. Until then the card says which address is waiting, and
**Change the address** or **Clear the address** replace or remove it. Clearing asks first, because
their next Google sign-in would then make a second, empty account. The field is not offered on an
account that already signs in with Google, and only the IT Manager sees it.

**Accounts** at the top of the page is the way back to the directory you came from. While the
page is loading it draws an outline of the cards that are coming, so nothing shifts under you.

Beneath the roles, **Membership** shows each term recorded against the account, its source and
whether it has been checked against the SU's list, and a Fellow has a **Fellowship** card. Further
down are the **Security** card, which only the IT Manager sees, **Merge into another account**,
and **Recent activity**: the last 25 entries from the audit trail naming this account, each read
as a sentence ("Role granted", "Signed in with a passkey"), marked "(done to them)" where
somebody else acted. Recent activity is filled in only for somebody who reads the audit trail.

## Granting and revoking a role

Grants expire at the committee year end (31 July) unless marked permanent. The full list of
roles and what each holds is on [Roles](/docs/people/roles).

1. In the **Roles** card, choose a **Role**. A role the account already holds live is not
   offered: renewing one is done from the register.
2. Choose **Until**. The committee year end is the default; **A date I pick** dates the grant to
   the end of that day in London, and **Further notice** makes it permanent. A permanent grant is
   the exception and shows in the IT Manager's monthly digest.
3. Write a **Note** if the grant needs explaining. It stays on the grant and never reaches the
   audit trail, which records only that one was written.
4. Press **Grant it**. The new grant appears with its expiry and its note.
5. To take a role away, press **Revoke** beside it, then **Revoke Box office** in the dialogue.
   The change takes effect on the person's next request.

## Security operations

![The security card with Sign out everywhere (1), Disable the account (2), Reset the authenticator (3), Erase this account (4) and the winning-account picker (5)](/images/docs/people/account-security.png)

Each of these takes effect the next time the person asks for a page, everywhere they are signed
in, and each is the IT Manager's. The whole card shows to nobody else: a Manager, Theatre
Manager or Training Manager reading an account sees the roles, the membership and the history,
and no security card at all.

1. **Sign out everywhere** ends every session on the account. Their roles and methods are
   untouched; they sign in again as normal.
2. **Disable the account** ends every session and refuses every sign-in path with the same
   answer as a wrong password, so somebody holding the credentials learns nothing. It asks first:
   press **Disable the account** in the dialogue, or **Back**. **Enable the account** reverses
   it and asks nothing, but the old sessions stay dead until the person signs in again.
3. **Reset the authenticator** clears the authenticator app, every recovery code and every
   session at once, for a person who has lost their phone. It asks first too: an officer
   holding a privileged role cannot sign in again until they set a new one up.
4. **Erase this account** is anonymisation, described below.
5. **Merge into another account** is described below.

Each of the first three says so in a notice at the corner of the screen once it has taken effect:
**Signed out everywhere**, **Account disabled** or **Account enabled**, and **Authenticator reset**.
Revoking a role from the roles card confirms the same way, with **Role revoked**.

## Erasing an account

Erasure is anonymisation in one transaction, the same path a person takes when they close their
own account. Bookings, training records and shifts stay as anonymous rows so the statistics
survive; the name, address and every piece of personal free text are rewritten or removed; the
access profile and other consented data are deleted outright. Nothing can undo it, and nothing
can write personal data back onto a closed account.

1. Press **Erase this account**. The button shows only to the IT Manager, and never on an
   account already erased.
2. Read the warning, then type the account's **email address** exactly to confirm. The button
   stays disabled until it matches.
3. Press **Erase the account**.

There is no console export of another person's data. Somebody who wants everything the theatre
holds on them downloads it themselves from their own account's security page, and that download
is audited (see [Your account](/docs/getting-started/your-account)).

## Merging duplicate accounts

The account you are looking at is always the losing side: it moves into the winner and is left
as a closed account, so anything that pointed at it still finds something. Bookings, training records,
shifts, memberships and role grants move; the loser's sign-in methods and authenticator are
retired, never combined with the winner's. The whole merge is one transaction, so a failure
changes nothing.

1. In **Merge into another account**, find the **winning account** by name, address or student
   number and choose it from the list, then press **Preview the merge**. Nothing changes yet.
2. The preview lists exactly what will move: the number of bookings, training records, shifts,
   memberships and role grants.
3. Type the **losing account's email** (the one on this page) to confirm, and press **Merge the
   accounts**.

## Shadow accounts

A shadow account has no way to sign in: no password, no Google link, no passkey. Three things
make one: adding somebody from the console before they choose a password, a ticket guest who
gave a name and an address at the box office, and the import, which brought over everyone the
old system knew, most of whom only ever bought a ticket. They are badged **Shadow**, never
Unverified, since nobody registered them to verify. The state ends by itself the moment a
password, a Google link or a passkey is written; nothing needs tidying.

## What happens next

Every action here is on the audit trail with who did it and to whom, and reads there as
**Account created from the console**, **Workspace address set for Google sign-in**, **Workspace
address for Google sign-in cleared**, **Role granted** (with its expiry, and whether it was
permanent), **Role grant renewed** (with the old expiry and the new one), **Role revoked**,
**Account disabled**, **Account enabled**, **Signed out everywhere by an officer**,
**Authenticator cleared by an officer**, **Account erased by an officer** or **Merged into
another account** with the counts that moved. A new account is emailed its set-password link
unless it is a Workspace address. The first Google sign-in with a set Workspace address reads as
**Google sign-in linked by a set Workspace address**, done by the person themselves. A revoked or lapsed grant opens nothing from the next page
onwards, and nothing waits for a nightly run.

## Related pages

- [Roles](/docs/people/roles)
- [Members](/docs/people/members)
- [Fellows](/docs/people/fellows)
- [Your account](/docs/getting-started/your-account)
- [The audit trail](/docs/system/audit-trail)
