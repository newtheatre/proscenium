# Development environment variables
DATABASE_URL="file:./dev.db"

# Production environment variables (set in Cloudflare Workers)
# DATABASE_URL is not needed in production as we use the D1 binding directly
# NODE_ENV="production"
