node_modules/
.nuxt/
.output/
.data/
.wrangler/
dist/
coverage/
*.log
.env
.env.*
!.env.example

# Local worker secrets for `wrangler dev` (0067's reproduction recipe). Never committed.
.dev.vars
.dev.vars.*
.DS_Store
migration/dumps/
migration/out/


# `bun run shots` writes here. The pictures are for looking at once, not for keeping.
.shots/

# Worktrees created for parallel work on the same clone
.claude/worktrees/
