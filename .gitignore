# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Cloudflare wrangler emulation
.wrangler
# Worktrees live inside the repo but are never part of it. Ignoring the
# directory rather than each one, which has been added twice already.
.claude/worktrees/